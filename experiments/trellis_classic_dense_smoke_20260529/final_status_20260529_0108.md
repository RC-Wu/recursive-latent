# TRELLIS classic dense mesh smoke status 2026-05-29 01:08 CST

## Environment path used
- Repo: `/mnt/beegfs/ruocheng/recursive_3d_generative_growth_20260507/repos/TRELLIS`
- Official weights snapshot: `/mnt/beegfs/ruocheng/recursive_3d_generative_growth_20260507/hf_home/hub/models--microsoft--TRELLIS-image-large/snapshots/25e0d31ffbebe4b5a97464dd851910efc3002d96`
- Working run root: `/mnt/beegfs/ruocheng/recursive_3d_generative_growth_20260507/trellis_classic_dense_smoke_20260529`

## Minimal environment fixes used
- `torchvision::nms` op was missing for the installed `torchvision`; the diagnostic script defines the op before importing torchvision.
- `kaolin` and `open3d` were only needed for imports/type annotations in the tested path, so local stubs under `$RUN/stubs` were used.
- For direct mesh decoder and SLat-flow smoke, the working Python was `/mnt/beegfs/xiyu/miniconda3/envs/vae-v4-t211/bin/python`, because it has both `flash_attn` and `spconv.pytorch`.
- Added stubs for `rembg`, `torchvision.transforms`, and `transformers` to avoid irrelevant image/text import blockers in this decoder-focused smoke.
- No TRELLIS source files were edited; all changes are diagnostic scripts/stubs under `$RUN`.

## Results
- Full package/pipeline import is now past the earlier blockers.
- `TrellisImageTo3DPipeline.from_pretrained(...)` successfully loaded all six official model components:
  `sparse_structure_decoder`, `sparse_structure_flow_model`, `slat_decoder_gs`, `slat_decoder_rf`, `slat_decoder_mesh`, `slat_flow_model`, plus a dummy image condition model.
- Direct mesh decoder smoke with real `slat_dec_mesh_swin8_B_64l8m256c_fp16` weights succeeded:
  - script: `$RUN/scripts/trellis_classic_mesh_decoder_smoke.py`
  - report: `$RUN/mesh_decoder_smoke.md`
  - output mesh: `$RUN/mesh_decoder_random_slat_smoke.ply`
  - forward time: `2.67s`
  - peak memory: `5.61GB`
  - exported mesh: `258` vertices, `468` faces.
- SLat-flow-to-mesh smoke with real pipeline weights and fixed nonempty sparse coords succeeded through SLat flow and mesh decode:
  - script: `$RUN/scripts/trellis_classic_slatflow_mesh_smoke.py`
  - report: `$RUN/slatflow_mesh_smoke.md`
  - one-step SLat flow + decode time: `2.60s`
  - peak memory: `7.64GB`
  - export failed only because the generated mesh object had empty tensor fields under dummy condition/fixed coords; the real model chain executed.

## Remaining gap
- A true image-conditioned dense mesh still needs a real DINOv2 image condition model path/cache. Current smoke monkeypatched `torch.hub.load` with a dummy DINO to avoid network dependency and focus on TRELLIS classic decoder/mesh path.
- The official sparse-structure flow with dummy condition produced empty coords in the 1-step smoke. That is expected for meaningless condition and does not invalidate the decoder smoke.
- To produce a useful dense mesh from an artist/image condition, next step is to locate or cache DINOv2 `dinov2_vitl14_reg` weights and run `TrellisImageTo3DPipeline.run(...)` with a real image and normal sampler steps.
