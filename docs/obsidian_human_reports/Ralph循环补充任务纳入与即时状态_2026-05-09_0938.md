---
title: Ralph循环补充任务纳入与即时状态 2026-05-09 09:38
date: 2026-05-09
tags:
  - recursive-3d-growth
  - trellis2
  - siga
  - ralph-loop
status: active
---

# Ralph 循环补充任务纳入与即时状态

## 已纳入的新增要求

1. **Reviewer 修改意见文件**已经作为正式子任务写入总计划文档。
   - 来源：`/Users/fanta/Downloads/论文修改意见与reviewer批评整理.md`
   - 处理原则：保留有用旧文本为 LaTeX 注释或草稿，不直接删除潜在可复用内容。
   - 当前判断：该文件的核心批评和我们自己的风险审计一致，即论文不能写成“传统程序化方法 + 生成模型”的拼接，而要写成一种生成模型原生的 sparse-latent recursive language / grammar execution system。

2. **同条件 depth / 参数可视化**已经作为方法展示任务纳入总计划。
   - 这类图不是消融，而是展示方法行为。
   - 要求固定 camera / material / guide，只改变 depth 或一个 grammar 参数。
   - 所有可进入论文或用户验收的图必须基于 mesh 或 textured mesh，不能用点云散点图。
   - 当前已有候选：vine depth、coral depth、coral density / compactness 参数控制。

3. **非树 / DLA / crystal 连通性问题**继续作为最高优先级研究风险。
   - 当前结论仍然保守：DLA / radial / bismuth / sci-fi 的 cache-selected textured GLB 能导出，但主图质量仍不足。
   - surface occupancy proxy 显示部分 repair 后可变成单连通，但视觉可能仍然块状或人工痕迹明显。
   - 论文里不能把这条线包装成已经完全解决，只能作为“连通性约束、projection、cache/repair 的必要性和边界”的证据，除非后续实验出现更好 case。

## 当前正在推进

### 远端 GPU 批处理

已在 `a100-2` 的 GPU `4,5,6,7` 启动新的 Trellis2 texture/PBR 批处理：

- run: `cache_repair_texture_20260509`
- 目标：检查修复/体素桥接后的非树结构是否能通过真实 Trellis2 纹理导出，并获得比之前更连通、更可用的 textured mesh。
- 四个 case：
  - repaired DLA voxel bridge + octopus guide
  - repaired radial voxel bridge + gear guide
  - repaired bismuth bridge-to-largest + bismuth guide
  - repaired sci-fi bridge-to-largest + gear guide

这组实验的判据不是“能导出 GLB 就算成功”，而是：

- occupancy / surface connectivity 不能碎；
- mesh 视觉不能像临时桥接出来的人工支架；
- 纹理/PBR 不能掩盖结构破碎；
- 至少能作为 supporting figure，才考虑进入论文。

### 本地并行子任务

当前四条本地子任务并行推进：

1. **论文方法子任务**：把 `main.tex` 中方法部分继续改成 compact state + rule template + recursive execution algorithm，加入 Algorithm 1/2 和覆盖传统 grammar 的 proof sketch。
2. **方法行为图子任务**：改进同条件不同 depth/参数的 mesh/textured-mesh 展示图，避免像消融图或 debugging 图。
3. **metric 子任务**：补足 connectivity / depth / 参数控制的评测协议、脚本和测试框架。
4. **理论语法子任务**：写更完整的中文 PS-RSLG / recursive generative grammar 框架说明，明确 IFS、L-system、space colonization、DLA、shape grammar、symmetry/crystal 的覆盖关系。

## 当前严格判断

论文目前最稳的主线仍然是：

> Grammar 控制递归结构，Trellis2 作为 sparse latent codec / local naturalizer / PBR exporter，projection 和 competition 作为递归执行语义的一部分，而不是最后的后处理。

可以放心推进的强证据：

- vine/root/tree/coral 的 depth 行为；
- projection-stabilized recursion 的连通性曲线；
- matched guide 传统基线纹理诊断；
- Trellis2 textured GLB/PBR 管线已经可用；
- 同条件 depth/参数图可作为“方法行为展示”。

必须谨慎的弱证据：

- DLA/crystal/Escher/infinite recursion 当前还不能作为主 claim；
- cache/repair 只能作为正在探索的机制和负/边界结果，除非后续出现更强视觉 case；
- baseline matrix 不能写成“我们在 connectivity 上全面击败传统方法”，只能写成公平性和任务定义验证。

## 下一步优先级

1. 等待 `cache_repair_texture_20260509` 完成，拉回本地用 Blender 渲染，严格肉眼 QA。
2. 若 repair-selected case 仍旧块状或语义弱，停止把 DLA/crystal 作为主图候选，转为 supplement/negative analysis。
3. 把 Algorithm 1/2 和 coverage proof sketch 合并进 LaTeX，并重新编译 PDF。
4. 把同条件 depth/参数图在论文中明确标注为 method behavior display。
5. 下一组 GPU 优先跑更可靠的 mesh-first connected scaffold + texture，而不是继续盲目追求难看的碎片 case。
