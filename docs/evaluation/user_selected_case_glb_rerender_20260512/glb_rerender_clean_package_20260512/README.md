# User-selected GLB rerender clean package 20260512

This folder contains direct GLB rerenders for the user-selected shortlist.

- Rendered GLB cases: 35
- Missing local GLB records: 5
- Per-case files under `images/<label>/`: `overview_raw.png`, `overview_gray_callout.png`, `zoom_01.png`
- All rendered images are 1200x1200 PNG.
- Background is pure white after transparent Blender render flattening.
- Callout boxes are gray and unlabeled.
- Main manifest: `rendered_glb_pack_manifest.json` with explicit `source_glb` fields.
- Gallery: `user_selected_case_glb_rerender_clean_gallery.html`

Missing local GLB cases were not rerendered from OBJ because the user explicitly requested direct GLB rerendering.
