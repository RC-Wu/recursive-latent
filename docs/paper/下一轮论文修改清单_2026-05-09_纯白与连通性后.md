# 下一轮论文修改清单 - 2026-05-09 纯白与连通性后

范围：仅基于 `/Users/fanta/Downloads/agent_revision_and_experiment_requirements.md`、`/Users/fanta/Downloads/论文修改意见与reviewer批评整理.md` 和当前 `paper_siga/main.tex` 做文本审计。下一步只列不需要新实验即可推进的改稿动作；不改 `main.tex` 和图。

## 已经修好的部分

- 摘要已经从“传统方法 + 生成模型互补”转向 `generation-model-native recursive grammar over sparse 3D latents`，并把 texture/PBR 降级为 selected export capability；仍保留 `\EvidencePending` 避免过强量化。
- Introduction 已经有清晰失败链：2D scaffold、direct sparse edit、global repair、final-only cleanup，并导出 grammar owns topology / frozen generator supplies local realization / projection stabilizes recursion。
- Contributions 已经合并 projection 贡献，texture 不再作为核心贡献。
- Method 已去掉旧的大状态八元组和大 grammar tuple，改成 `z_d=(\mathcal V_d,\mathbf F_d,\mathcal A_d)`、rule template、compact transition Eq. `\ref{eq:compact-recursive-transition}` 和 algorithm boxes。
- Naturalization 已经写成 masked flow / local sampler，并明确不是 global repair。
- Projection 已经比旧稿具体：有 connectivity graph、root-attached components、bridge budget、handle update、validation、objective 和 final-only cleanup insufficiency。
- Experiments 已经加入 claim map、protocol separation、main/figures-only/supplement boundary、occupancy vs raw face components 的区分。
- 纯白图和连通性主线已经进入正文：`fig:connected-best-expansion-textured`、`fig:projection-ablation-mesh`、`fig:coral-depth-textured`、`fig:surface-voxel-connectivity` 等 caption 都在弱化纹理并强调 connected scaffold / diagnostic boundary。
- Traditional baseline 已不再是 strawman：`fig:traditional-texture-baseline` caption 明确 classical supports 也可 texture transfer。

## 仍然有风险的部分

- 正文仍然过长且像 working draft：保留了大量 `% Previous ... kept for revision trace` 注释，投稿前必须清掉；可先做一次“删注释不改内容”的清洁。
- Related Work 仍有 `Infinite and World-Scale Generation` 主文小节；最新约束建议移出主 related work，最多在 Discussion 保留一句。
- `Classical Systems as Limits` 仍在 Method 主线中，篇幅较长，会打断核心方法；应压缩为一段定位，详细推导放 supplement。
- `Symmetry, Cache, and Recursion Scope` 与 `Recursive Effective Resolution and Infinite Refinement` 仍有强潜力但证据不足；需要把 claim 改成 conservative finite-depth / qualitative behavior，避免“exceed one-shot resolution”读起来像已证明。
- 还有明显 draft 词：`draft`、`candidate`、`smoke test`、`stress-test`、`Current`、`boundary case` 等。部分可保留为内部注释，但 visible caption/table 中应改成正式论文措辞。
- 主文 figure 数量太多，且多个 gallery/guide sweep 仍在正文：这会直接违反“正文主 claim + 最多 2 页 figures-only pages + supplement”的组织目标。
- 指标风险仍在：LCR/occupancy connectedness 被清楚限定了，但 branch/tip/root semantic metrics、mesh validity、runtime、seed/root variation 还没闭环。
- `Table~\ref{tab:texture-qa}` 仍把 visually weak / holes / holed fragments 放在主表里，容易让 reviewer 觉得结果不成熟；应把主表改成 export diagnostics summary，弱案例下沉。
- Teaser caption 仍说 radial and DLA panels illustrate breadth and boundary cases；如果 teaser 是第一眼主图，应避免把 boundary cases 放进 teaser 或 caption。

## 下一轮可直接改 `main.tex` 的位置

1. `Abstract`，第 26-31 行附近：保留当前 framing，但把 “Current evidence shows...” 改成更正式的 “Our current evaluation indicates...”；保留 `\EvidencePending` 直到 matched baseline matrix 完成。
2. `Introduction`，第 49-67 行附近：整体已经可用，只需压缩 10-15%，删重复解释；第 65 行 contribution 的 `matched baselines, root/tip metrics` 保留为 pending。
3. `Related Work`，第 76-95 行附近：合并 `Training-Free 3D Editing` 与 `Structure Control and Growth Evaluation` 为 `Generative Control and Editing`；删除或迁移 `Infinite and World-Scale Generation`。
4. `Method / Core State and Handles`，第 120-155 行附近：修正符号风险，`R_d` 现在表示 root anchors，但后文 `\mathcal R_d` 表示 selected rules，建议把 root anchors 改成 `\mathcal V_d^{root}`，避免 reviewer 误读。
5. `Rule Templates`，第 157-215 行附近：公式里 `\Delta\mathcal V\cup B\in\mathcal Z_{\mathrm{adm}}` 仍有类型风险；应写成 “there exists features/anchors such that `z'=(...) \in \mathcal Z_{\mathrm{adm}}`” 或定义 support-level admissibility。
6. `Projection-Stabilized Execution`，第 217-327 行附近：保留结构，但把 boxed algorithm 的引用前后文字再压缩；projection steps 中 “optionally weld/remesh/insert bridge geometry” 要加一句“only as validated projection implementation detail”，避免像手工修复。
7. `Classical Systems as Limits`，第 330-393 行附近：主文压缩到一段 + 小表；IFS/L-system/space-colonization/DLA 详细公式移 supplement。
8. `Recursive Effective Resolution`，第 420-427 行附近：把 “can exceed a single global pass” 改成 “is designed to allocate capacity locally”；保留 `\EvidencePending`，不要在正文 claim measured advantage。
9. `Experiments` 开头，第 468-494 行附近：保留 claim map，但 caption 中 `current paper draft` 改成正式语气；`smoke-test exports` 改为 `diagnostic exports`。
10. `Results / Current Boundaries`，第 543-546 行附近：这两段很好，但应提前成为 figure-selection policy，然后据此删/迁正文 gallery。

## 下一轮最该处理的 figures/tables

- `fig:teaser`：如果继续使用当前 teaser，移除 radial/DLA boundary panels 或把 caption 改得更像 selected scope overview；第一图不要主动暴露“boundary cases”。
- `fig:method-overview`：保留为主方法图，但检查是否仍是 PPT 式总览；caption 中 “lower panels show classical systems...” 可能支持把 classical limits 移到 supplement 后同步删。
- `tab:claim-map-current`：保留，但删 “current paper draft”；把 pending 实验列留作 `Evidence pending`，不要像 internal tracker。
- `fig:result-matrix`、`fig:depth-parameter-mesh-showcase`、`fig:crystal-stage4-guide-sweep`、`fig:vine-stage5-guide-sweep`：优先移 figures-only 或 supplement；正文最多保留一个 distilled claim-specific panel。
- `fig:connected-best-expansion-textured` 与 `fig:projection-ablation-mesh`：这是纯白与连通性后最强正文视觉证据，应保留，但 caption 要更短、更论文式。
- `fig:dla-bridge-smoke-rerun`：改名和 caption 中的 `smoke test`；主文若空间紧张，移 supplement，正文只保留一句 negative diagnostic。
- `tab:projection-ablation`：保留，但 caption 已很长；下一轮压缩，并在 Metrics 中集中定义 raw mesh vs occupancy proxy。
- `tab:texture-qa`：主文只留 selected export compatibility summary；weak/hole/mismatch 行放 supplement 或改成 diagnostic subsection。

## 必须等实验或新统计后才能改成最终 claim

- matched same-root baseline matrix：sparse-latent vs mesh-space recursion、one-shot Trellis2、direct sparse edit、global/masked repair、final-only/per-depth projection。
- branch/tip/root semantic metrics：root reachability、orphan-tip rate、endpoint/path-to-root、branch count/length/angle 等。
- naturalization ablation：rule-only、encode/decode only、no naturalization、masked naturalization、global repair、weak blend、with/without projection。
- effective resolution 证据：single-pass vs recursive-window 对照、terminal detail、token density、depth/detail retention。
- root/seed robustness：multiple roots、multiple seeds、success/failure rate、mean/std。
- runtime/memory：per-depth encode/decode/projection/texture cost、token/mesh growth、GPU memory、hardware。
- material/export diagnostics：GLB import/export rate、PBR channel completeness、UV/seam/render warnings；只能支持 asset readiness，不能支持 structural correctness。

## 建议下一步顺序

1. 先做 `main.tex` 文本清洁：删 revision-trace 注释、替换 draft/candidate/smoke 等词、压缩超长 captions。
2. 再做正文结构收缩：移出 world-scale、classical limits 长推导、gallery/guide sweep、boundary diagnostics。
3. 然后统一图表边界：主文只留 projection ablation、纯白 connected scaffold、selected coral/crystal/vine、traditional fair baseline、texture QA summary。
4. 最后补 `EvidencePending` 和 `ExpFigTODO` 的精确位置，确保所有强 claim 都有 pending 标记或被改成保守表述。
