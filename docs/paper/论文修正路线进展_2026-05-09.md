---
title: 论文修正路线进展 2026-05-09
date: 2026-05-09
tags:
  - recursive-3d-generative-growth
  - paper-revision
  - SIGGRAPH-Asia
status: in-progress
---

# 论文修正路线进展 2026-05-09

## 本轮修改范围

本轮只修改了论文修正路线相关文件：

- `paper_siga/main.tex`
- `paper_siga/references.bib`
- `docs/paper/论文修正路线进展_2026-05-09.md`
- Obsidian 同步笔记

没有修改 `visuals`、实验脚本或其他 agent 正在整理的图集/进度文档。

## main.tex 已完成修改

1. 在 preamble 中加入了两个显式标注宏：
   - `\EvidencePending{...}`
   - `\ExpFigTODO{...}`

2. 重写并收紧了 abstract：
   - 核心从 projection 转为 `generation-model-native recursive grammar over sparse 3D latents`。
   - projection 被定义为执行语义，用于保持递归状态 admissible。
   - texture/PBR 只作为 selected projected mesh 的 export capability。
   - 对尚未闭环的最终量化改进加入了 `\EvidencePending{...}`。

3. 重写 introduction 主逻辑：
   - 先讲 recursive algorithms / recursive structures 的图形学和建模动机。
   - 再讲传统 L-system、IFS、DLA、space colonization、shape grammar/CGA 的结构优势和资产化不足。
   - 再区分 set-latent / end-to-end object latent 与 sparse structured latent 两类现代 3D generator。
   - 明确 Trellis/Trellis2 对本文有用的是 sparse token support、latent features、mesh-to-sparse encoding、sparse-to-mesh decoding、masked local sampling、texture/PBR export path。
   - 把 2D scaffold、direct sparse edit、global repair、final-only cleanup 的失败链条写成方法动机，并标注仍需统一诊断实验。

4. 收紧 contributions：
   - 合并了 projection-stabilized execution 与 explicit projection procedure。
   - texture/PBR 从核心 contribution 中移出，改成 selected export capability。
   - 对最终 quantitative claims、matched baselines、root/tip metrics 标了 `\EvidencePending{...}`。

5. 改写 Related Work 主线：
   - procedural/recursive modeling 不再被写成 strawman。
   - 加入 shape grammar / CGA 相关引用。
   - 加入 3DShape2VecSet，明确 set-latent/object-latent 与 sparse structured latent 的差别。
   - 对 sparse-latent 相比 set-latent/object-latent 的实证比较标了 `\EvidencePending{...}`。

6. 扩展 claim-to-evidence 表：
   - 增加 sparse substrate over object latents 的 claim/evidence/protocol/must-not-claim 行。
   - 增加 branch semantics remain usable 的 claim/evidence/protocol/must-not-claim 行。
   - 对缺实验的比较和 branch semantics 指标显式标注 `\EvidencePending{...}`。

7. 增加主文 / figures-only / supplement 边界说明：
   - 主文不能依赖 appendix 图表编号。
   - claim-bearing figures 与 broad guide sweep、full matrix、cache/LOD、负例诊断分开。
   - 用 `\ExpFigTODO{...}` 标出提交前需要把当前 gallery-heavy figure set 拆成主文 claim figures、最多两页 figures-only pages 和 supplement diagnostics。

8. 保留被替换的大段文字：
   - abstract 旧版本保留为 LaTeX comment。
   - introduction 被替换正文保留为 LaTeX comment。
   - related work 被替换段落保留为 LaTeX comment。

## references.bib 已完成修改

新增或补充了以下引用条目：

- `stiny1972shapegrammars`
- `mueller2006procedural`
- `shape2vecset2023`

用途分别对应 shape grammars、CGA/procedural building modeling、set-latent/object-latent 3D representation 对比。

## 编译状态

已在 `paper_siga` 下运行指定命令：

```bash
PATH=/Users/fanta/Library/TinyTeX/bin/universal-darwin:$PATH latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
```

结果：

- `latexmk` 退出码为 0。
- `main.pdf` 已生成。
- macOS metadata 显示 `main.pdf` 为 25 页，大小 16,598,839 bytes。
- 当前 `main.log` 中未检出 `undefined` citation/reference 或 LaTeX fatal/error。

仍存在的非阻塞 warning：

- ACM 模板提示图片缺少 description。
- ACM 模板提示 keywords / CCS / printacmref。
- 若干 underfull/overfull box。
- BibTeX 对少数既有条目的 publisher/address/pages 有 warning。

## 仍需实验支持的强 claim

1. sparse structured latent 相比 set-latent / end-to-end object latent / mesh-only route 更适合递归执行。
   - 当前已写成方法动机。
   - 仍需 controlled comparison 或更强文献/实验支撑。

2. per-depth projection 的最终量化优势。
   - 当前已有 conservative vine/tree projection ablation。
   - 仍需 same-root / same-seed / same-depth 的 tree/root/vine matched matrix，才能写最终 improvement numbers。

3. 2D scaffold、direct sparse edit、global repair、final-only cleanup 的失败链条。
   - 当前已写入 introduction。
   - 仍需统一 root/depth protocol 的 diagnostic failure panels。

4. root / branch / tip 语义保持。
   - 当前 connected occupancy 证据较强。
   - 仍需 root reachability、orphan-tip rate、endpoint、path-to-root 等语义指标。

5. effective structural resolution 超过 single global pass。
   - 当前保留为方法解释。
   - 需要 matched single-pass versus recursive-window comparison 后才能写成 measured advantage。

6. 主文图集边界。
   - 当前已用 `\ExpFigTODO{...}` 标出。
   - 后续需要把 gallery-heavy results 拆成主文 claim figures、figures-only pages、supplement diagnostics。

## 当前建议的下一步

优先补齐 `tree/root/vine` matched projection matrix 和 root/tip 语义指标；这两项会直接决定 abstract/contributions 里所有 `\EvidencePending{...}` 能否被替换为投稿级量化 claim。
