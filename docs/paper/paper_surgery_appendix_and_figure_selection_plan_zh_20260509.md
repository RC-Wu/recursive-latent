# 下一轮论文手术计划：Appendix 与 Figure Selection

日期：2026-05-09

输入依据：

- `/Users/fanta/code/agent/Code/recursive_3d_generative_growth/paper_siga/main.tex`
- `/Users/fanta/code/agent/Code/recursive_3d_generative_growth/docs/paper/reviewer_critique_current_application_zh_20260509.md`

本文档只给下一轮写作和排版手术计划，不修改 `main.tex`、实验脚本或已有 plan。

## 0. 总体手术目标

下一轮不是继续加图，而是把当前研究草稿压成一篇 reviewer 能快速判断 claim 的 SIGA 论文。

主文只保留三条证据链：

1. PS-RSLG 的形式化贡献：grammar owns support/anchors，Trellis2 只做 sparse state、masked local prior、decode/re-encode、selected material export。
2. 核心实验证据：per-depth projection 比 direct recursion 和 final-only cleanup 更能稳定 finite-depth connected recursive support。
3. 有边界的资产出口：selected projected meshes 可以进入 Trellis2 texture/PBR export，但这不是 material recursion consistency，也不是 raw GLB topology clean。

所有 breadth、gallery、guide sweep、弱 case、传统系统长证明、状态报告式材料，转到 appendix/supplement。

## 1. 主文建议保留的 Figure/Table 清单

### Main Figure 1：Teaser

保留：`figures/teaser_candidate_layout_v3_20260509.png`

当前位置：`Figure~\ref{fig:teaser}`

主文角色：第一眼展示“recursive asset + selected texture/export + boundary cases”，但必须降低 claim 口气。

下一轮 caption 收紧方向：

- 首句直接说这是 selected projected recursive assets，而不是全方法质量概览。
- 明确绿色/texture panels 是 true Trellis2 textured GLB，programmatic PBR strip 只用于 geometry zoom。
- 删掉“breadth”暗示，改成“included to indicate operator scope and current boundaries”。
- 不要在 teaser caption 里解释太多协议，详细协议转 supplement。

建议 caption 核心句：

> Selected projected recursive assets generated by PS-RSLG. Trellis2 texture/PBR export is shown only for panels marked as textured GLB; programmatic PBR and neutral renders are used where geometry inspection is the relevant evidence. The figure previews the finite-depth connected-recursion claim and its current boundary cases.

### Main Figure 2：Method Overview

保留：`figures/method_system_ps_rslg_v3_20260509.pdf`

当前位置：`Figure~\ref{fig:method-overview}`

主文角色：唯一方法总图。

下一轮 caption 收紧方向：

- 保留 projection inside transition。
- 删除“lower panels show classical procedural systems arise as limiting cases and where current experiments provide evidence”这类信息过载句，classical systems 放到 appendix。
- caption 不要重复正文完整 operator chain。

建议 caption 核心句：

> PS-RSLG executes recursive programs over sparse generator-native states. Rules propose support and anchors; masked Trellis2 operations naturalize local edits; projection is applied before re-encoding, so connected admissible support becomes a recursive invariant rather than a final cleanup target.

### Main Table 1：Rule Families

保留但压缩：当前 `Table~\ref{tab:rule-families}`

主文角色：让 reader 看到 grammar 是 compact language，不是单个 vine trick。

下一轮处理：

- 表格保留 5 行以内：grow、branch、frontier/attach、copy、refine/material。
- `cache` 不放主文，转 appendix。
- `material` 可以并入 refine/material 或放脚注，避免读者误以为 material recursion 已证明。

caption 收紧方向：

- 强调“implemented/evaluated families are a subset”。
- 不说所有 family solved。

### Main Figure 3：Claim-Aligned Metric Summary

保留：`figures/claim_aligned_metric_summary_20260509.png`

当前位置：`Figure~\ref{fig:claim-aligned-metrics}`

主文角色：指标口径防误解，是 reviewer 批评里 P0 的防线。

下一轮 caption 收紧方向：

- 开头说 primary structural metric 是 occupancy connected support。
- raw face components 和 welded diagnostics 只作为 diagnostics。
- 红叉 DLA bridge-ablation 保留，显示负结果边界。
- 删除“current draft”字样，改成 paper-ready 口吻。

建议 caption 核心句：

> Structural evaluation separates occupancy connectedness, welded mesh diagnostics, and raw GLB face connectivity. The defended claim is connected recursive support under the occupancy proxy; raw GLB face components are reported as export diagnostics rather than hidden failures.

### Main Figure 4：Projection Ablation Mesh Contact Sheet

保留：`figures/projection_ablation_mesh_contact_20260509.png`

当前位置：`Figure~\ref{fig:projection-ablation-mesh}`

主文角色：核心结果图。必须排在 Results 第一小节附近，比 curves 更重要。

下一轮 caption 收紧方向：

- 直接对齐 claim：direct recursion / final-only / per-depth。
- 不要只说“visualizes trend”；要说 projection 的递归位置区别。
- 加一句：aggressive fork rows reveal boundary, not failure hidden.

建议 caption 核心句：

> Matched projection ablation. Direct recursion leaves fragments available to later rules; final-only projection cleans only the terminal mesh; per-depth projection decodes, projects, and re-encodes before subsequent handle selection. Conservative programs show the clearest stabilization, while fork variants expose the stability-expression boundary.

### Main Table 2：Projection Ablation

保留：当前 `Table~\ref{tab:projection-ablation}`

主文角色：核心数值表。

下一轮处理：

- 保留四行即可：vine compete d3、tree compete d3、vine compete-fork d3、tree compete-fork d3。
- 表内列名收紧，避免读者把 raw mesh diagnostics 当 primary occupancy claim：
  - `Direct raw comps.`
  - `Direct raw LCR`
  - `Final kept comps.`
  - `Per-depth kept comps.`
  - `Per-depth raw LCR`
- caption 第一行说明“raw mesh diagnostic table; occupancy-primary summary is Figure 3”。

### Main Figure 5：Projection Depth Curves

保留但可降为单栏：`figures/projection_depth_curves_20260508.pdf`

当前位置：`Figure~\ref{fig:projection-depth-curves}`

主文角色：补充 projection ablation 的跨 stage 趋势。

下一轮处理：

- 如果版面紧张，保留为单栏；token growth curves 转 supplement。
- caption 要把“largest connected component ratio”标成 diagnostic，不要与 occupancy-primary metric 混写。

### Main Figure 6：Selected Texture/PBR Export

保留二选一，优先保留：`figures/vine_stage5_guide_sweep_20260509.png`

备选：`figures/vine_depth_textured_strip_20260509_vizworker.png`

主文角色：只证明 selected projected meshes can enter texture/PBR export。

建议选择：

- 若主文只允许一张 texture 图，选 `vine_stage5_guide_sweep_20260509.png`，因为它闭合 depth-5 metric claim，并且固定 geometry、只换 guide，claim 边界最清楚。
- `vine_depth_textured_strip` 转 supplement，作为 depth progression visual。

caption 收紧方向：

- 明确 fixed projected stage-5 mesh。
- 明确 guide sweep，不是 ablation。
- 明确 raw GLB face diagnostics remains caveat。

### Main Figure 7：Same-Condition Control

保留一张，优先保留：`figures/depth_parameter_main_candidate_20260509.pdf`

备选：`figures/depth_parameter_mesh_showcase_zoom_20260509.pdf`

主文角色：method behavior evidence，不是核心 ablation。

建议选择：

- 如果需要更紧凑主文，保留 `depth_parameter_main_candidate`，把 zoom 版转 supplement。
- 如果版面允许双栏图且局部结构很关键，保留 zoom 版，但 caption 必须更短。

caption 收紧方向：

- 使用“same-condition behavior display”。
- 不写“control proves”。
- coral-density 只说 endpoint qualitative behavior，不说 calibrated monotonic law。

### Main Table 3：Texture Export QA

保留但大幅压缩：当前 `Table~\ref{tab:texture-qa}`

主文角色：把 texture/export 从视觉主观判断中分离出来。

下一轮处理：

- 主文保留 5-6 行代表样例：
  - vine/root compete
  - tree compete
  - crown portal 或 snow arch portal（二选一）
  - public-guide vine
  - public-guide porous/crystal
  - DLA radial boundary case（如果需要显式边界）
- 其余完整表转 supplement。
- 列保留：Case、Export、Shape tokens、PBR tokens、Evaluation role。
- `GLB MB` 可转 supplement，主文不重要。

## 2. 应转 Supplement 的 Figure/Table 清单

### S-Fig 1：Sparse Token Growth Curves

转 supplement：`figures/space_competition_depth_curves_tokens_20260508.pdf`

原因：解释 stability-expression boundary 有用，但主文已有 projection curves 和 ablation。主文只用一句引用 supplement：“token-budget behavior is reported in Supplementary Fig. Sx”。

caption 修改：

- 强调 token consumption diagnostic。
- 不把 fork/portal/scale-down 说成主文成功结果。

### S-Fig 2：Traditional Space-Colonization Baseline Render

转 supplement：`figures/space_colonization_blender_contact_sheet.png`

原因：传统 baseline 需要公平展示，但主文 Related Work/Experiments 只能短说，不应占主文双栏图。

caption 修改：

- 说明 structural procedural baseline, untextured mesh renderer。
- 不说“visually schematic”太重，避免 strawman 感。

### S-Fig 3/S-Fig 4：Traditional Texture Baseline

转 supplement：

- `figures/traditional_baseline_texture_20260509.png`
- `figures/traditional_baseline_texture_matched_guide_20260509.png`

原因：它们支持“texture transfer alone does not ensure connected recursive asset”，但主文一个压缩 paragraph 足够。

主文只保留一句：

> Supplementary matched-guide texture baselines show that coloring procedural supports is not sufficient to satisfy the same occupancy-connected asset criteria.

caption 修改：

- 删除“not yet full matched same-root baseline matrix”这种草稿语气。
- 指标口径写清：occupancy proxy vs rendered GLB。

### S-Fig 5：Result Matrix

转 supplement：`figures/result_matrix_mesh_20260509.png`

原因：这是状态总览，不是单一 claim evidence。主文保留会让 reviewer 以为每格都要 defend。

caption 修改：

- 标题改成“Qualitative operator gallery and protocol labels”。
- 保留 green/blue/gray protocol separation。
- 明确不是 quantitative evidence，不是所有 panel 都是 success case。

### S-Fig 6：Vine Zoom

转 supplement：`figures/vine_zoom_in_multiscale_20260509.png`

原因：很好看但 claim 辅助性强。可作为 local structure inspection。

caption 修改：

- 只说 fixed programmatic PBR for geometry inspection。
- 不把它放在 texture/PBR 证据链里。

### S-Fig 7：Vine Depth Textured Strip

转 supplement，除非主文不用 guide sweep：`figures/vine_depth_textured_strip_20260509_vizworker.png`

原因：与 stage5 guide sweep 重复。主文只保留一个 texture evidence figure。

### S-Fig 8：Depth/Parameter Zoom Showcase

转 supplement：`figures/depth_parameter_mesh_showcase_zoom_20260509.pdf`

原因：如果主文保留 `depth_parameter_main_candidate`，zoom 版进 supplement。

### S-Fig 9/S-Fig 10：Bismuth/Crystal Guide Sweep

转 supplement：

- `figures/bismuth_depth_textured_showcase_20260509.png`
- `figures/crystal_stage4_guide_sweep_20260509.png`

原因：non-tree texture/export breadth，目前不能升级成主文核心 claim。它们容易触发 reviewer 追问 physical crystal growth 和 raw topology。

caption 修改：

- crystal/bismuth 均写“crystal-inspired connected scaffold”。
- 禁止使用 physical simulation 语气。
- pyrite 明确为 supporting/boundary, not primary evidence。

### S-Fig 11：Coral Textured Depth

视版面决定，建议转 supplement：`figures/coral_depth_textured_showcase_20260509.png`

原因：主文若已保留 same-condition control 中的 coral row，则不再需要单独 coral texture depth 图。

可在主文 Results boundary paragraph 引用 supplement。

### S-Fig 12：Full Depth/Guide Gallery

转 supplement：`figures/depth_parameter_mesh_showcase_20260509.png`

原因：当前 caption 已经承认 intended as supplementary method-control evidence。不要放主文。

### S-Fig 13：Coral Density Extreme

转 supplement，或合并进 main same-condition figure：`figures/coral_density_extreme_texture_20260509.png`

原因：它强于旧弱参数图，但仍是 qualitative endpoint sweep。主文只需在 selected same-condition figure 里保留一行，完整 endpoint sweep 放 supplement。

### S-Table 1：Matched Structural Baseline Matrix

转 supplement：`\input{drafts/baseline_matrix_table_20260509.tex}`

原因：主文不应铺开传统 baseline 数表。主文只需写一段说明 same-root/same-seed/same-depth/same-renderer protocol，并在 supplement 给完整矩阵。

注意：不要在下一轮修改 `drafts/baseline_matrix_table_20260509.tex`，除非用户另行授权。

### S-Table 2：Full Texture QA Table

转 supplement：当前完整 `Table~\ref{tab:texture-qa}`

原因：主文只保留 representative rows。完整导出状态、GLB MB、guide variants 放 supplement。

## 3. Classical Systems Coverage 的压缩方案

当前 `Classical Systems as Limits` 在主文展开过长，包括 IFS、L-systems、space colonization、DLA/frontier、shape grammars/symmetry 的段落与公式。下一轮应从主文压缩成一段加一张小表，把证明和公式移 appendix。

### 主文保留内容

保留位置：Method 末尾或 Related Work/Method 之间。

主文建议结构：

1. 一段文字：
   - 说明 classical procedural systems are limiting cases of PS-RSLG when learned naturalization, material export, and projection are disabled or simplified。
   - 说明这是 coverage/semantic unification，不是 dominance claim。
2. 一个 5 行小表：
   - IFS：transform-copy + union merge + identity sampler。
   - L-system：symbol/handle rewrite + turtle frame realization。
   - Space colonization：attractor-guided grow proposal + occupancy exclusion。
   - DLA/frontier：hitting-distribution attach proposal + adjacency/bridge certificate。
   - Shape/symmetry grammar：typed split/extrude/copy handles + orbit scheduler。

主文必须删掉：

- 每个 classical system 的证明段落。
- Hutchinson iteration 详细 induction。
- L-system standard parallel derivation 证明。
- DLA hitting probability 公式。
- symmetry equivariance 不等式。

### Appendix A 建议内容：Classical Systems as PS-RSLG Limits

appendix 中保留现在的完整材料，但做三点清理：

- 标题从“Classical Systems as Limits”改成“Classical Systems as Degenerate PS-RSLG Instances”。
- 每节最后加一句“不构成视觉质量或性能优越性声明”。
- symmetry 段明确 exact equivariance 需要 codec/grid/projection/sampler 都 commute，当前只 report approximation metrics。

## 4. Results 从状态报告改成 Claim-Based Organization

Reviewer 文档已经指出 Results 仍偏状态报告。下一轮 Results 建议改为 5 个 claim 小节，每节只回答一个问题。

### Result 1：Direct and Final-Only Cleanup Fail as Recursive Operators

目的：先解释为什么 final-only 不够。

保留证据：

- Projection ablation table。
- Projection ablation mesh figure。

写作方式：

- 不说“the current implementation shows”。
- 改成“Because orphan fragments can be selected as later handles, cleanup must occur before the next rule application.”
- direct/final-only 是 operator-level failure，不只是 final mesh ugliness。

需要清理的状态报告语气：

- “The strongest empirical claim is not that...”
- “in the cleanest vine case...”

改法：

- 先给 claim，再给数字。
- 数字后明确 raw mesh diagnostic，不混 primary occupancy metric。

### Result 2：Per-Depth Projection Stabilizes Conservative Growth

目的：主 claim。

保留证据：

- Claim-aligned metrics。
- Projection depth curves。
- Projection ablation table/figure。

写作方式：

- 使用“conservative competition programs”限定范围。
- 明确“finite-depth connected support”，不要说 general recursive generation solved。
- fork variants 作为 boundary 放本节末尾。

要删或改的状态报告语气：

- “current stability-expression boundary”可以保留，但要定义一次。
- “strongest stability baseline”改成“most stable tested setting”。

### Result 3：Classical Baselines Separate Structural Control from Appearance Export

目的：不 strawman，同时不给传统 baseline 太多主文空间。

保留证据：

- 主文只保留一句或短段。
- 传统 baseline render/texture figures 转 supplement。
- baseline matrix table 转 supplement。

写作方式：

- 承认 classical baselines are structurally strong。
- 说明 paper 比较点不是“我们比 L-system 更连通”，而是“recursive support can live inside generator-native sparse state and selected projected meshes can be exported with texture/PBR”。

要删或改的状态报告语气：

- “visually schematic and untextured”要谨慎，改成“do not include learned local surface/material priors under the same asset export path”。

### Result 4：Selected Projected Meshes Enter Texture/PBR Export

目的：texture/export claim。

保留证据：

- Main texture figure：优先 stage5 guide sweep。
- 压缩 texture QA table。

写作方式：

- 开头句必须限制：
  - “We evaluate texture export only after projection, and only as asset-readiness evidence.”
- 不说 “supports PBR story for non-tree” 这类泛化语气。
- public-guide rows 是 compatibility test，不是 standalone method contribution。

需要清理的状态报告语气：

- “Projection-stabilized meshes can enter...”可以保留。
- “extend the evidence to non-tree connected scaffolds”主文删除或转 supplement。
- “true Trellis2 textured GLB exports”保留，但不要反复强调。

### Result 5：Behavior Displays and Failure Boundary

目的：把 depth/parameter、DLA、radial、crystal、gallery 都放到边界和行为观察，不让 reviewer 当核心 claim 打。

保留证据：

- 主文一张 same-condition figure。
- supplement 中 full gallery 和 non-tree guide sweeps。

写作方式：

- “These displays show qualitative response under fixed conditions; they are not calibrated ablations.”
- DLA/crystal/fork/radial/echo 都归入 boundary diagnostics。
- 明确 negative/partial results support why attachment feasibility and projection are necessary。

要删或改的状态报告语气：

- “Figure result-matrix is retained...”删除，因为 result matrix 不应留主文。
- “aggressive fork, radial, echo...”保留为 boundaries，但不要列太长。

## 5. Caption 统一收紧规则

下一轮所有主文 caption 执行以下规则：

1. 每个 caption 第一行必须说明该图支持哪个 claim。
2. 每个 caption 最多保留一个 caveat；更多 caveat 放正文或 supplement。
3. 删除草稿词：
   - current draft
   - not yet
   - needs tuning
   - strongest current candidate
   - status overview
   - figure-quality judgement
4. 保留必要边界词：
   - selected
   - finite-depth
   - occupancy-primary
   - diagnostic
   - guide sweep, not ablation
   - export compatibility, not topology proof
5. 不在 caption 中反复解释 Trellis2 是 frozen；方法图和正文解释一次即可。
6. 避免 “PBR story”“asset-ready” 的宽泛表达；改成 “texture/PBR export compatibility”。

## 6. Result 正文需要清理的具体句式

以下是下一轮应替换的语气模式。

### 状态报告式

原语气：

- “The strongest empirical claim is not that all grammars become clean assets...”
- “Figure X is retained as...”
- “This result is included as...”
- “current implementation”
- “current evidence”

建议改成：

- “We evaluate the claim under conservative competition programs and report fork variants as boundary cases.”
- “The ablation separates where projection is applied in the recursive transition.”
- “The display is used only as same-condition behavior evidence.”
- “The tested setting...”
- “The reported evidence...”

### 过宽 claim

原语气风险：

- “supports the scaffold-connected recursion and PBR-export story”
- “extends evidence to non-tree”
- “material transfer alone does not make a scaffold asset-ready”

建议改成：

- “shows export compatibility for this selected projected mesh”
- “provides supplementary non-tree stress-test evidence”
- “does not satisfy the same connected-support diagnostic under the reported proxy”

### 指标混写

必须固定写法：

- “occupancy-primary connected support”用于主 structural claim。
- “raw GLB face components”用于 export/topology diagnostic。
- “welded face diagnostics”用于 mesh-quality diagnostic。
- 不要把 raw face components 的 `comps` 和 occupancy components 混在同一句没有限定词的句子里。

## 7. 建议的主文最终版图表数量

目标主文图表预算：

- Figures：7 张以内。
- Tables：3 张以内。

建议最终主文：

1. Figure 1 Teaser。
2. Figure 2 Method overview。
3. Table 1 Rule families。
4. Figure 3 Claim-aligned metrics。
5. Figure 4 Projection ablation mesh。
6. Table 2 Projection ablation numeric table。
7. Figure 5 Projection depth curves。
8. Figure 6 Stage-5 vine guide sweep 或 vine depth textured strip。
9. Figure 7 Same-condition depth/parameter behavior。
10. Table 3 Condensed texture export QA。

如果版面进一步压缩：

- 删除 Figure 5 projection depth curves，转 supplement。
- 删除 Table 3 texture QA，改为正文一句 + supplement full table。
- Rule families table 合并到 Method paragraph。

## 8. 建议的 Appendix/Supplement 结构

### Appendix A：Classical Procedural Systems as Degenerate PS-RSLG Instances

内容：

- IFS proof sketch。
- L-system/turtle-frame reduction。
- Space colonization anchor proposal。
- DLA/frontier hitting distribution。
- Shape grammar/symmetry orbit scheduling。
- 明确这是 semantic coverage，不是 superiority claim。

### Appendix B：Projection and Connectivity Diagnostics

内容：

- occupancy proxy definition。
- raw face components。
- welded face diagnostics。
- root reachability proxy。
- why final-only cleanup lacks recursive invariant。

这部分承接主文 Figure 3 和 Table 2 的指标细节。

### Appendix C：Experimental Protocols

内容：

- same-root/same-seed/same-depth/same-renderer protocol。
- Blender render protocol。
- Trellis2 texture/PBR export protocol。
- public guide image usage说明。
- export validation fields。

### Supplementary Figures

建议顺序：

1. Token growth curves。
2. Traditional procedural structural baseline renders。
3. Traditional texture baseline unmatched/matched guide。
4. Full result matrix with protocol labels。
5. Vine multi-scale zoom。
6. Vine textured depth progression。
7. Depth/parameter zoom showcase。
8. Bismuth depth textured showcase。
9. Crystal guide sweep。
10. Coral depth textured showcase。
11. Full depth/guide gallery。
12. Coral density endpoint sweep。

### Supplementary Tables

1. Full matched structural baseline matrix。
2. Full texture QA/export table。
3. Optional: per-family failure/boundary diagnostics table。

## 9. 一句话手术原则

主文只 defend “finite-depth projection-stabilized connected recursive support + selected texture/PBR export compatibility”。凡是让 reviewer 以为论文在 claim “all recursive operators solved, physical DLA/crystal solved, raw GLB topology clean, material recursion proven, or superior to all classical systems” 的图、caption、段落，都必须移到 appendix/supplement 或改成 boundary diagnostic。
